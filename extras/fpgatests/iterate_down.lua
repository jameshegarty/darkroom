(terralib.loadfile("test.t"))()
import "darkroom"

local inp = im(x,y) testinput(x*2,y*2) end

test(im(x,y) 
     iterate i=-1,0 A=darkroom.gatherColumn(inp,i,1,-1,0,-1,0) reduce(sum)
       A[0]+A[1]
     end
end)
